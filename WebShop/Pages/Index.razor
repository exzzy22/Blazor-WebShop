@page "/"

<TopCategories/>
<Carousel CarouselModel="Carousel" Title="NEW PRODUCTS" />
<HotDeal/>
<Carousel CarouselModel="Carousel" Title="BEST SELLER" />
<WidgetSlick/>

@code
{
    [Inject]
    public IJSRuntime JS { get; set; } = null!;
    [Inject]
    public IApiService ApiService { get; set; } = null!;
    public CarouselVM Carousel { get; set; } = new();

    protected override async Task OnInitializedAsync()
    {
        Carousel = await ApiService.GetCarouselTopSelling(2, 8);
    }

    //protected override async Task OnAfterRenderAsync(bool firstRender)
    //{
    //    Carousels = await ApiService.GetCarouselTopSelling(4, 8);
    //}
}